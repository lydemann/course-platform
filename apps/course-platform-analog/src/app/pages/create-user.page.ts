import { RouteMeta } from '@analogjs/router';
import { CreateUserComponent } from '@course-platform/course-client/feature';

export const routeMeta: RouteMeta = {
  title: 'Create User',
  providers: [],
};

export default CreateUserComponent;
